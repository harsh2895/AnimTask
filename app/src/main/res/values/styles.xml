<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:buttonStyle">@string/app_name</item>
    </style>
        <style name="cus" parent="@android:style/Widget.Button">
            <item name="android:textColor">#00fffb</item>
            <item name="android:background">#006aff</item>
            <item name="android:backgroundSplit">@color/colorPrimaryDark</item>
            <item name="android:typeface">monospace</item>
        </style>
</resources>
